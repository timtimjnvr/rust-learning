- create a new project

```$ cargo new```

- build project (binary will be in target/debug directory)

```$ cargo build```

- build + run 

```$ cargo run```

- build without producing a binary

```$ cargo check```

- build with optimizations (binary will be in target/release directory)

```$ cargo build --release```
